<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Receptionist</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
  <!-- <%-include('admin.ejs')%> -->
  <div class="main-content">
    <h2>Edit Receptionist</h2>
    <form action="/admin/edit-reception/<%= reception.reception_id %>" method="POST">
      <label>Name</label>
      <input type="text" name="reception_name" value="<%= reception.reception_name %>" required>
      
      <label>Contact</label>
      <input type="text" name="reception_contact" value="<%= reception.reception_contact %>">

      <label>Status</label>
      <select name="status">
        <option value="Active" <%= reception.status === 'Active' ? 'selected' : '' %>>Active</option>
        <option value="Inactive" <%= reception.status === 'Inactive' ? 'selected' : '' %>>Inactive</option>
      </select>

      <button type="submit">Update</button>
    </form>
  </div>
</body>
</html>