<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Doctor Dashboard</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <!-- Font Awesome for icons -->
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    crossorigin="anonymous"
    referrerpolicy="no-referrer"
  />

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      display: flex;
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f6f9;
      color: #333;
    }

    .sidebar {
      width: 250px;
         background-color: #007bff;
      color: white;
      height: 100vh;
      padding: 30px 20px;
      display: flex;
      flex-direction: column;
    }

    .sidebar h3 {
      font-size: 22px;
      margin-bottom: 30px;
      color: #ffffff;
      text-align: center;
    }

    .sidebar ul {
      list-style: none;
      padding-left: 0;
    }

    .sidebar ul li {
      margin-bottom: 10px;
    }

    .sidebar ul li > a,
    .dropdown-toggle {
      color: #d1d1d1;
      text-decoration: none;
      font-size: 16px;
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 10px 15px;
      border-radius: 5px;
      transition: background 0.2s;
      cursor: pointer;
    }

    .sidebar ul li > a:hover,
    .dropdown-toggle:hover {
      background-color: #495057;
      color: #ffffff;
    }

    .submenu {
      list-style: none;
      padding-left: 20px;
      margin: 5px 0 10px;
      display: none;
      animation: fadeIn 0.3s ease-in-out;
    }

    .submenu li a {
      font-size: 14px;
    }

    .submenu.show {
      display: block;
    }

    .content {
      flex: 1;
      padding: 40px;
    }

    .welcome {
      background-color: #e9ecef;
      padding: 20px;
      border-left: 5px solid #6f42c1;
      font-size: 20px;
      margin-bottom: 20px;
      border-radius: 4px;
    }

    .logout {
      background-color: #dc3545;
      color: white !important;
      margin-top: auto;
      padding: 10px 15px !important;
      border-radius: 5px;
      display: inline-block;
    }

    .logout:hover {
      background-color: #bd2130;
    }

    @media (max-width: 768px) {
      body {
        flex-direction: column;
      }

      .sidebar {
        width: 100%;
        height: auto;
      }

      .content {
        padding: 20px;
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-5px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>

<body>
  <div class="sidebar">
    <h3><i class="fas fa-user-md"></i> Doctor Panel</h3>
    <ul>
      <li>
        <div class="dropdown-toggle" onclick="toggleDropdown('patientMenu')">
          <i class="fas fa-users"></i> Patient
        </div>
        <ul class="submenu" id="patientMenu">
          <li><a href="/doctor/patient-visited"><i class="fas fa-check-circle"></i> Visited</a></li>
          <li><a href="/doctor/patient-not-visited"><i class="fas fa-times-circle"></i> Not Visited</a></li>
          <li><a href="/doctor/assign-patient"><i class="fas fa-user-plus"></i> Assign Patient</a></li>
        </ul>
      </li>

      <li>
        <a href="/logout" class="logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
      </li>
    </ul>
  </div>

  <div class="content">
    <div class="welcome">
      👋 Welcome, Dr. <%= user.username %>
    </div>
    <p>Please choose an option from the left sidebar to begin managing your patients.</p>
  </div>

  <script>
    function toggleDropdown(id) {
      const menu = document.getElementById(id);
      menu.classList.toggle("show");
    }
  </script>
</body>
</html>
